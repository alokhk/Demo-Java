<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ak86.jwala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IsolationActivity"></activity>
        <activity android:name=".AddPeopleActivity" />
        <activity android:name=".QuarantineActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ManageUserActivity" />
        <activity android:name=".admin.CompanyActivity" />
        <activity android:name=".admin.RankActivity" />
        <activity android:name=".CoronaActivity" />
        <activity android:name=".LandingActivity"></activity>
        <activity android:name=".PasswordResetActivity" />
        <activity android:name=".CreateNewUser" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>